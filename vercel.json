{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/generate.js": {
      "memory": 1024,
      "maxDuration": 60,
      "includeFiles": "fonts/**"
    }
  },
  "rewrites": [
    { "source": "/v/:slug", "destination": "/api/view?slug=:slug" }
  ]
}
